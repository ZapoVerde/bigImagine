{
  "name": "bigbrain",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "orchestrator",
    "plugins/*",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=@bigbrain/orchestrator && npm run build --workspaces --if-present",
    "check": "npm run build --workspace=@bigbrain/orchestrator && npm run check --workspaces --if-present",
    "verify": "npm run build && npm run verify --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "@types/node": "^22.0.0"
  }
}
